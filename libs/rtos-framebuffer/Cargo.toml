[package]
name = "rtos-framebuffer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
default = []

[dependencies]
uefi = { version = "0.35", default-features = false }
rtos-types = { path = "../../libs/rtos-types" }
