[package]
name = "serial-writer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]

[profile.dev]
panic = "abort"
lto = false
codegen-units = 1

[profile.release]
panic = "abort"
lto = false
codegen-units = 1
